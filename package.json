{
  "name": "@immerse/i18n",
  "version": "1.6.0",
  "description": "Shared UI strings for the Immerse mobile app and immerse-web.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "bin"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepare": "npm run build",
    "pretest": "npm run build",
    "test": "node --test dist/__tests__/*.test.js",
    "typecheck": "tsc -p tsconfig.build.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "~5.9.2"
  },
  "bin": {
    "immerse-i18n-check": "bin/check-untranslated.mjs"
  }
}
